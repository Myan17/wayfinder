#!/usr/bin/env bash
# Push only as the operator's own GitHub account (scripts/check_gh_account.py explains why).
# Install once per clone/worktree:  git config core.hooksPath .githooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
exec python3 scripts/check_gh_account.py
